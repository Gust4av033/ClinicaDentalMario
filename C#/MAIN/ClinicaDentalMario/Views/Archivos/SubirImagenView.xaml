<UserControl x:Class="ClinicaDentalMario.Views.Archivos.SubirImagenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Background="Transparent">

    <Grid>
        <Border Background="White" CornerRadius="15" Padding="30" MaxWidth="500" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D1D8E0" Opacity="0.6"/>
            </Border.Effect>

            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <iconPacks:PackIconMaterial Kind="FileImagePlusOutline" Width="28" Height="28" Foreground="#8E44AD" Margin="0,0,10,0"/>
                    <TextBlock Text="Anexar Archivo Clínico" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                </StackPanel>

                <TextBlock Text="{Binding MensajeError}" Foreground="#E74C3C" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,15">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding MensajeError}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="1. Selecciona la radiografía o fotografía:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Background="#F4F6F7" Padding="10">
                        <TextBlock Text="{Binding NuevaImagen.RutaArchivo}" Foreground="#34495E" FontStyle="Italic" TextTrimming="CharacterEllipsis"/>
                    </Border>

                    <Button Grid.Column="2" Command="{Binding ExplorarArchivoCommand}" Background="#34495E" Foreground="White" BorderThickness="0" Padding="15,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="FolderSearch" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="Explorar..." VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <TextBlock Text="2. Descripción de la imagen:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock Text="Ej: Radiografía Panorámica Inicial, Foto de Frente, etc." Foreground="#95A5A6" FontSize="11" Margin="0,0,0,5" FontStyle="Italic"/>
                <TextBox Text="{Binding NuevaImagen.Descripcion, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="14" Padding="8" BorderBrush="#8E44AD" BorderThickness="1.5" 
                         Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,25"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding CancelarCommand}" Background="Transparent" Foreground="#7F8C8D" BorderBrush="#BDC3C7" BorderThickness="1" Height="40" Padding="20,0" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="Cancelar" FontSize="14" FontWeight="SemiBold"/>
                    </Button>

                    <Button Command="{Binding GuardarCommand}" Background="#8E44AD" Foreground="White" BorderThickness="0" Height="40" Padding="25,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="CloudUpload" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Guardar en Expediente" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>