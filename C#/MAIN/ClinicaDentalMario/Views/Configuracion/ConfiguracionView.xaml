<UserControl x:Class="ClinicaDentalMario.Views.Configuracion.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Background="Transparent">

    <Border Background="White" CornerRadius="15" Margin="10" Padding="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#D1D8E0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                <iconPacks:PackIconMaterial Kind="Cog" Width="28" Height="28" Foreground="#34495E" Margin="0,0,10,0"/>
                <TextBlock Text="Configuración y Catálogos" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>

            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="Foreground" Value="#7F8C8D"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0,0,0,3"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#2980B9"/>
                                <Setter Property="BorderBrush" Value="#2980B9"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="Tratamientos y Precios">
                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="1.2*"/>
                        </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0" ItemsSource="{Binding ListaTratamientos}" 
                                  SelectedItem="{Binding TratamientoSeleccionado}"
                                  AutoGenerateColumns="False" CanUserAddRows="False" Background="White" 
                                  BorderThickness="1" BorderBrush="#E1E5EA" RowHeight="35" FontSize="13">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nombre del Tratamiento" Binding="{Binding Nombre}" Width="*"/>
                                <DataGridTextColumn Header="Precio Base ($)" Binding="{Binding PrecioBase, StringFormat=C}" Width="120" FontWeight="Bold" Foreground="#27AE60"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Column="2" Background="#F8F9FA" Padding="20" CornerRadius="10" BorderBrush="#E1E5EA" BorderThickness="1" VerticalAlignment="Top">
                            <StackPanel>
                                <TextBlock Text="Agregar Nuevo Tratamiento" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                                <TextBlock Text="Nombre:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding NuevoNombreTratamiento, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="6" Margin="0,0,0,15" BorderBrush="#BDC3C7"/>

                                <TextBlock Text="Precio Sugerido ($):" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding NuevoPrecioBase, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="6" Margin="0,0,0,20" BorderBrush="#BDC3C7"/>

                                <Button Command="{Binding GuardarTratamientoCommand}" Height="38" Background="#2980B9" Foreground="White" BorderThickness="0" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="ContentSave" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="Guardar en Catálogo" VerticalAlignment="Center" FontWeight="Bold"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding EliminarTratamientoCommand}" Margin="0,10,0,0" Height="30" Background="Transparent" Foreground="#E74C3C" BorderBrush="#E74C3C" BorderThickness="1" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                    <TextBlock Text="Eliminar Seleccionado" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem Header="Doctores y Personal">
                    <Grid Margin="0,20,0,0">
                        <TextBlock Text="¡Módulo de doctores en construcción para la próxima fase!" Foreground="#7F8C8D" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </TabItem>

                <TabItem Header="Métodos de Pago y Estados">
                    <Grid Margin="0,20,0,0">
                        <TextBlock Text="¡Módulo de estados en construcción!" Foreground="#7F8C8D" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </TabItem>

            </TabControl>
        </Grid>
    </Border>
</UserControl>