<UserControl x:Class="ClinicaDentalMario.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="10,10,10,25">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial Kind="ViewDashboard" Width="28" Height="28" Foreground="#2C3E50" Margin="0,0,10,0"/>
                    <TextBlock Text="Resumen General del Día" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Command="{Binding AbrirConfiguracionCommand}" HorizontalAlignment="Right" Height="40" Background="#34495E" Foreground="White" BorderThickness="0" Padding="15,0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Cog" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Configurar Catálogos" VerticalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="10,0,10,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="25">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#D1D8E0" Opacity="0.5"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <iconPacks:PackIconMaterial Kind="CashMultiple" Width="24" Height="24" Foreground="#27AE60" Margin="0,0,10,0"/>
                            <TextBlock Text="Ingresos Totales (Hoy)" FontSize="16" FontWeight="SemiBold" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding IngresosHoy, StringFormat={}{0:C2}}" FontSize="32" FontWeight="Bold" Foreground="#27AE60"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="15" Padding="25">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#D1D8E0" Opacity="0.5"/>
                    </Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <iconPacks:PackIconMaterial Kind="CalendarCheck" Width="24" Height="24" Foreground="#2980B9" Margin="0,0,10,0"/>
                            <TextBlock Text="Citas para Hoy" FontSize="16" FontWeight="SemiBold" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CitasHoy}" FontSize="32" FontWeight="Bold" Foreground="#2980B9"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="2" Margin="10,0,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="1.2*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#D1D8E0" Opacity="0.5"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <iconPacks:PackIconMaterial Kind="CalendarClock" Foreground="#27AE60" Width="22" Height="22" Margin="0,0,8,0"/>
                            <TextBlock Text="Próximas Citas (Hoy)" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                        </StackPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding ListaCitasHoy}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" BorderThickness="0" RowHeight="45" FontSize="14" HeadersVisibility="Column">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F6FA"/>
                                    <Setter Property="Foreground" Value="#7F8C8D"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="10,10"/>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Hora" Binding="{Binding Hora}" Width="100" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Paciente" Binding="{Binding Paciente}" Width="*"/>
                                <DataGridTextColumn Header="Tratamiento" Binding="{Binding Tratamiento}" Width="150"/>
                                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="120" Foreground="#2980B9"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="2">

                    <Border Background="#FDEDEC" CornerRadius="12" Padding="15" Margin="0,0,0,15" BorderBrush="#F5B7B1" BorderThickness="1">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <iconPacks:PackIconMaterial Kind="AlertCircleOutline" Foreground="#C0392B" Width="20" Height="20" Margin="0,0,8,0"/>
                                <TextBlock Text="Saldos Pendientes" FontSize="16" FontWeight="Bold" Foreground="#C0392B"/>
                            </StackPanel>
                            <ItemsControl ItemsSource="{Binding Morosos}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Paciente}" FontWeight="SemiBold" Foreground="#34495E"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Saldo, StringFormat=C}" FontWeight="Bold" Foreground="#C0392B"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Border Background="#FEF9E7" CornerRadius="12" Padding="15" BorderBrush="#F9E79F" BorderThickness="1">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <iconPacks:PackIconMaterial Kind="CakeVariant" Foreground="#D4AC0D" Width="20" Height="20" Margin="0,0,8,0"/>
                                <TextBlock Text="Cumpleañeros" FontSize="16" FontWeight="Bold" Foreground="#9A7D0A"/>
                            </StackPanel>
                            <ItemsControl ItemsSource="{Binding Cumpleaneros}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,5">
                                            <TextBlock Text="{Binding Paciente}" FontWeight="Bold" Foreground="#34495E"/>
                                            <TextBlock Text="{Binding Fecha}" FontSize="12" Foreground="#7F8C8D"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
    