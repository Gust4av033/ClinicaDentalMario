<Window x:Class="ClinicaDentalMario.Views.Login.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Acceso al Sistema - CDMario Dental" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F5F6FA">

    <Grid>
        <Border Background="White" CornerRadius="15" Padding="30" Margin="25" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D1D8E0" Opacity="0.6"/>
            </Border.Effect>

            <StackPanel>
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                    <iconPacks:PackIconMaterial Kind="ToothOutline" Width="48" Height="48" Foreground="#2980B9" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="CDMario Dental" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" TextAlignment="Center"/>
                    <TextBlock Text="Sistema de Gestión Clínica" FontSize="12" Foreground="#7F8C8D" TextAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="Usuario:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox FontSize="14" Padding="10" BorderBrush="#BDC3C7" BorderThickness="1" Margin="0,0,0,15" Text="Administrador (Demo)">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="Contraseña:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <PasswordBox FontSize="14" Padding="10" BorderBrush="#BDC3C7" BorderThickness="1" Margin="0,0,0,25" Password="••••••••">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>

                <Button Command="{Binding AccederCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                        Background="#27AE60" Foreground="White" BorderThickness="0" Height="45" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Login" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Acceder al Sistema" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>