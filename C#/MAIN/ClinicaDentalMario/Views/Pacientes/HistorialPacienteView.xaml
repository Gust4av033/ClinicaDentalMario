<UserControl x:Class="ClinicaDentalMario.Views.Pacientes.HistorialPacienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:archivos="clr-namespace:ClinicaDentalMario.Views.Archivos"
             Background="Transparent">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding VolverCommand}" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,15,0">
                    <iconPacks:PackIconMaterial Kind="ArrowLeft" Width="28" Height="28" Foreground="#7F8C8D"/>
                </Button>
                <iconPacks:PackIconMaterial Kind="FolderAccount" Width="30" Height="30" Foreground="#2C3E50" Margin="0,0,10,0"/>
                <TextBlock Text="Expediente Clínico" FontSize="26" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Command="{Binding AbrirNuevaConsultaCommand}" HorizontalAlignment="Right" Height="40" Background="#3498DB" Foreground="White" BorderThickness="0" Padding="15,0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Nueva Consulta" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#D1D8E0" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="{Binding PacienteActual.NombreCompleto}" FontSize="22" FontWeight="Bold" Foreground="#2980B9"/>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <iconPacks:PackIconMaterial Kind="Phone" Foreground="#7F8C8D" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding PacienteActual.Telefono}" Foreground="#34495E" FontSize="14" Margin="0,0,20,0" VerticalAlignment="Center"/>

                    <iconPacks:PackIconMaterial Kind="CardAccountDetails" Foreground="#7F8C8D" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding PacienteActual.DUI}" Foreground="#34495E" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="Foreground" Value="#7F8C8D"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0,0,0,3"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="#8E44AD"/>
                            <Setter Property="BorderBrush" Value="#8E44AD"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Historial Clínico">
                <Border Background="White" CornerRadius="0,8,8,8" Padding="15">
                    <DataGrid ItemsSource="{Binding HistorialConsultas}" AutoGenerateColumns="False" 
                              CanUserAddRows="False" Background="White" BorderThickness="0" 
                              RowBackground="White" AlternatingRowBackground="#F9FAFC"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E1E5EA"
                              HeadersVisibility="Column" RowHeight="45" FontSize="14">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F6FA"/>
                                <Setter Property="Foreground" Value="#7F8C8D"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,12"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding FechaConsulta, StringFormat=dd/MM/yyyy}" Width="120" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Motivo" Binding="{Binding MotivoConsulta}" Width="*"/>
                            <DataGridTextColumn Header="Diagnóstico" Binding="{Binding Diagnostico}" Width="*"/>
                            <DataGridTemplateColumn Header="Acción" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.VerDetalleConsultaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Ver detalle">
                                            <iconPacks:PackIconMaterial Kind="Eye" Foreground="#3498DB"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <TabItem Header="Odontograma Digital">
                <Border Background="White" CornerRadius="0,8,8,8" Padding="40">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <iconPacks:PackIconMaterial Kind="Tooth" Width="60" Height="60" Foreground="#BDC3C7" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <TextBlock Text="Módulo de Odontograma" FontSize="20" FontWeight="Bold" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                        <TextBlock Text="Próximamente..." FontSize="14" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </TabItem>

            <TabItem Header="Galería de Imágenes">
                <archivos:ImagenesPacienteView DataContext="{Binding GaleriaVM}"/>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>