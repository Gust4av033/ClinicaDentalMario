<UserControl x:Class="ClinicaDentalMario.Views.Pacientes.NuevoPacienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
        <Border Background="White" CornerRadius="15" Padding="30" MaxWidth="800">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#D1D8E0" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Command="{Binding CancelarCommand}" Background="Transparent" BorderThickness="0" Margin="0,0,15,0" Cursor="Hand" ToolTip="Regresar al listado">
                            <iconPacks:PackIconMaterial Kind="ArrowLeft" Width="28" Height="28" Foreground="#95A5A6"/>
                        </Button>
                        <iconPacks:PackIconMaterial Kind="FolderAccount" Width="28" Height="28" Foreground="#3498DB" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Apertura de Expediente Clínico" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="{Binding MensajeError}" Foreground="#E74C3C" FontWeight="SemiBold" FontSize="14" Margin="0,5,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding MensajeError}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding MensajeError}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,15">
                        <TextBlock Text="Nombre Completo:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NuevoPaciente.NombreCompleto, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,15">
                        <TextBlock Text="Dirección:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NuevoPaciente.Direccion, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,15">
                        <TextBlock Text="Fecha de Nacimiento:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding NuevoPaciente.FechaNacimiento}" FontSize="14" Padding="5" BorderBrush="#BDC3C7"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,15">
                        <TextBlock Text="Sexo:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox SelectedValue="{Binding NuevoPaciente.Sexo}" SelectedValuePath="Content" FontSize="14" Padding="5" BorderBrush="#BDC3C7">
                            <ComboBoxItem Content="Masculino"/>
                            <ComboBoxItem Content="Femenino"/>
                            <ComboBoxItem Content="Otro"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,15">
                        <TextBlock Text="Número de DUI:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NuevoPaciente.DUI, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    </StackPanel>
                    <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,15">
                        <TextBlock Text="Teléfono:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NuevoPaciente.Telefono, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,0,15">
                        <TextBlock Text="En caso de emergencia llamar a:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NuevoPaciente.ContactoEmergencia, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    </StackPanel>
                    <StackPanel Grid.Row="4" Grid.Column="2" Margin="0,0,0,15">
                        <TextBlock Text="Teléfono de Emergencia:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NuevoPaciente.TelefonoEmergencia, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <TextBlock Text="Motivo de consulta:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NuevoHistorial.MotivoConsulta, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1" 
                             Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>

                    <TextBlock Text="Antecedentes médicos:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NuevoHistorial.AntecedentesMedicos, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1" 
                             Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>

                    <TextBlock Text="Antecedentes odontológicos:" Foreground="#7F8C8D" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NuevoHistorial.AntecedentesOdontologicos, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="14" Padding="8" BorderBrush="#BDC3C7" BorderThickness="1" 
                             Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Command="{Binding CancelarCommand}" Background="Transparent" Foreground="#7F8C8D" BorderBrush="#BDC3C7" BorderThickness="1" Height="40" Padding="20,0" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="Cancelar" FontSize="14" FontWeight="SemiBold"/>
                    </Button>

                    <Button Command="{Binding GuardarCommand}" Background="#27AE60" Foreground="White" BorderThickness="0" Height="40" Padding="25,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="ContentSave" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Guardar Expediente" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </Grid>
        </Border>
    </ScrollViewer>
</UserControl>