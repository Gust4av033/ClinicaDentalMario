<UserControl x:Class="ClinicaDentalMario.Views.Reportes.ReporteProductividadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Background="{StaticResource BgWindowBrush}"
             Foreground="{StaticResource TextPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Button Command="{Binding VolverCommand}" Background="Transparent" BorderThickness="0" Margin="0,0,15,0" VerticalAlignment="Top" Cursor="Hand"><Border Background="{StaticResource BgControlBrush}" CornerRadius="6" Padding="8"><iconPacks:PackIconMaterial Kind="ArrowLeft" Width="20" Height="20"/></Border></Button>
            <StackPanel Grid.Column="1"><TextBlock Text="{Binding Titulo}" FontSize="28" FontWeight="Bold"/><TextBlock Text="Tratamientos iniciados en el periodo, volumen clínico y valor proyectado." FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/></StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,0,0,18">
            <Border Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="15" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,15,0"><TextBlock Text="Desde" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5"/><DatePicker SelectedDate="{Binding FechaInicio}" Width="145" Height="34"/></StackPanel>
                    <StackPanel Margin="0,0,15,0"><TextBlock Text="Hasta" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5"/><DatePicker SelectedDate="{Binding FechaFin}" Width="145" Height="34"/></StackPanel>
                    <Button Command="{Binding GenerarReporteCommand}" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" Padding="16,8" VerticalAlignment="Bottom" Cursor="Hand"><StackPanel Orientation="Horizontal"><iconPacks:PackIconMaterial Kind="Magnify" Margin="0,0,7,0"/><TextBlock Text="Analizar" FontWeight="SemiBold"/></StackPanel></Button>
                </StackPanel>
            </Border>
            <UniformGrid Columns="3">
                <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,10,0"><StackPanel><TextBlock Text="TRATAMIENTOS" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text="{Binding TotalTratamientosPeriodo}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/></StackPanel></Border>
                <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,10,0"><StackPanel><TextBlock Text="TIPOS REALIZADOS" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text="{Binding TiposTratamiento}" FontSize="25" FontWeight="Bold"/></StackPanel></Border>
                <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource SuccessBrush}" BorderThickness="0,0,0,4" CornerRadius="8" Padding="14"><StackPanel><TextBlock Text="VALOR PROYECTADO" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text="{Binding TotalProyectadoPeriodo, StringFormat=C2}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/></StackPanel></Border>
            </UniformGrid>
        </StackPanel>

        <Border Grid.Row="2" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid ItemsSource="{Binding ListaProductividad}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tratamiento" Binding="{Binding Tratamiento}" Width="*"/>
                    <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="130"/>
                    <DataGridTextColumn Header="Valor proyectado" Binding="{Binding IngresoProyectado, StringFormat=C2}" Width="160"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0"><Button Command="{Binding ExportarPdfCommand}" Background="#E74C3C" Foreground="White" BorderThickness="0" Padding="18,9" Cursor="Hand"><StackPanel Orientation="Horizontal"><iconPacks:PackIconMaterial Kind="FilePdfBox" Margin="0,0,7,0"/><TextBlock Text="Exportar PDF" FontWeight="Bold"/></StackPanel></Button></StackPanel>
    </Grid>
</UserControl>
