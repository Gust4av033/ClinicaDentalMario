<UserControl x:Class="ClinicaDentalMario.Views.Reportes.ReporteSaldosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Background="{StaticResource BgWindowBrush}"
             Foreground="{StaticResource TextPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Button Command="{Binding VolverCommand}" Background="Transparent" BorderThickness="0" Margin="0,0,15,0" VerticalAlignment="Top" Cursor="Hand">
                <Border Background="{StaticResource BgControlBrush}" CornerRadius="6" Padding="8"><iconPacks:PackIconMaterial Kind="ArrowLeft" Width="20" Height="20"/></Border>
            </Button>
            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding Titulo}" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="Pacientes con montos pendientes, total abonado y saldo actual por cobrar." FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,18">
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0"><StackPanel><TextBlock Text="PACIENTES CON SALDO" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text="{Binding PacientesConSaldo}" FontSize="25" FontWeight="Bold"/></StackPanel></Border>
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0"><StackPanel><TextBlock Text="TOTAL CARGOS" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text="{Binding TotalCargos, StringFormat=C2}" FontSize="25" FontWeight="Bold"/></StackPanel></Border>
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0"><StackPanel><TextBlock Text="TOTAL PAGADO" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text="{Binding TotalPagado, StringFormat=C2}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/></StackPanel></Border>
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="#E74C3C" BorderThickness="0,0,0,4" CornerRadius="8" Padding="15"><StackPanel><TextBlock Text="SALDO PENDIENTE" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/><TextBlock Text="{Binding SaldoPendiente, StringFormat=C2}" FontSize="25" FontWeight="Bold" Foreground="#E74C3C"/></StackPanel></Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,18">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0"><TextBlock Text="Buscar paciente" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5"/><TextBox Text="{Binding TerminoBusqueda, UpdateSourceTrigger=PropertyChanged}" Width="300" Height="34" VerticalContentAlignment="Center"/></StackPanel>
                <Button Command="{Binding GenerarReporteCommand}" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" Padding="16,8" VerticalAlignment="Bottom" Cursor="Hand"><StackPanel Orientation="Horizontal"><iconPacks:PackIconMaterial Kind="Magnify" Margin="0,0,7,0"/><TextBlock Text="Actualizar" FontWeight="SemiBold"/></StackPanel></Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
            <DataGrid ItemsSource="{Binding ListaSaldos}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Paciente" Binding="{Binding NombreCompleto}" Width="2*"/>
                    <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="120"/>
                    <DataGridTextColumn Header="Cargos" Binding="{Binding TotalCargos, StringFormat=C2}" Width="120"/>
                    <DataGridTextColumn Header="Pagado" Binding="{Binding TotalPagado, StringFormat=C2}" Width="120"/>
                    <DataGridTextColumn Header="Saldo pendiente" Binding="{Binding SaldoPendiente, StringFormat=C2}" Width="140">
                        <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="#E74C3C"/></Style></DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Command="{Binding ExportarPdfCommand}" Background="#E74C3C" Foreground="White" BorderThickness="0" Padding="18,9" Cursor="Hand"><StackPanel Orientation="Horizontal"><iconPacks:PackIconMaterial Kind="FilePdfBox" Margin="0,0,7,0"/><TextBlock Text="Exportar PDF" FontWeight="Bold"/></StackPanel></Button>
        </StackPanel>
    </Grid>
</UserControl>
