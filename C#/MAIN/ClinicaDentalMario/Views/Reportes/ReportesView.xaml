<UserControl x:Class="ClinicaDentalMario.Views.Reportes.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{StaticResource BgWindowBrush}"
             Foreground="{StaticResource TextPrimaryBrush}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,28">
            <TextBlock Text="{Binding Titulo}" FontSize="32" FontWeight="Bold"/>
            <TextBlock Text="Información clínica y administrativa lista para consultar, filtrar y exportar." FontSize="15" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="3" Rows="2" VerticalAlignment="Top">
            <Button Command="{Binding AbrirReportePacientesCommand}" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="10" Height="210" Cursor="Hand">
                <Button.Template><ControlTemplate TargetType="Button"><Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="20"><StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><Border Background="#1A3498DB" Width="58" Height="58" CornerRadius="29" Margin="0,0,0,14"><iconPacks:PackIconMaterial Kind="AccountGroup" Width="29" Height="29" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="Pacientes" FontSize="18" FontWeight="Bold"/><TextBlock Text="Listado general, menores, nuevos registros y datos de contacto." TextWrapping="Wrap" TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/></StackPanel></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/><Setter TargetName="border" Property="Background" Value="{StaticResource BgControlHoverBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Button.Template>
            </Button>

            <Button Command="{Binding AbrirReporteAgendaCommand}" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="10" Height="210" Cursor="Hand">
                <Button.Template><ControlTemplate TargetType="Button"><Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="20"><StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><Border Background="#1AE67E22" Width="58" Height="58" CornerRadius="29" Margin="0,0,0,14"><iconPacks:PackIconMaterial Kind="CalendarCheck" Width="29" Height="29" Foreground="#E67E22" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="Citas y Asistencia" FontSize="18" FontWeight="Bold"/><TextBlock Text="Atendidas, canceladas, ausencias y porcentaje de asistencia." TextWrapping="Wrap" TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/></StackPanel></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="#E67E22"/><Setter TargetName="border" Property="Background" Value="{StaticResource BgControlHoverBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Button.Template>
            </Button>

            <Button Command="{Binding AbrirReporteTratamientosCommand}" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="10" Height="210" Cursor="Hand">
                <Button.Template><ControlTemplate TargetType="Button"><Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="20"><StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><Border Background="#1A8E44AD" Width="58" Height="58" CornerRadius="29" Margin="0,0,0,14"><iconPacks:PackIconMaterial Kind="Tooth" Width="29" Height="29" Foreground="#9B59B6" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="Tratamientos" FontSize="18" FontWeight="Bold"/><TextBlock Text="Productividad, tratamientos más realizados y volumen proyectado." TextWrapping="Wrap" TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/></StackPanel></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="#9B59B6"/><Setter TargetName="border" Property="Background" Value="{StaticResource BgControlHoverBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Button.Template>
            </Button>

            <Button Command="{Binding AbrirReporteIngresosCommand}" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="10" Height="210" Cursor="Hand">
                <Button.Template><ControlTemplate TargetType="Button"><Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="20"><StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><Border Background="#1A27AE60" Width="58" Height="58" CornerRadius="29" Margin="0,0,0,14"><iconPacks:PackIconMaterial Kind="CashRegister" Width="29" Height="29" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="Ingresos / Corte de Caja" FontSize="18" FontWeight="Bold"/><TextBlock Text="Pagos recibidos, métodos de pago y total recaudado por periodo." TextWrapping="Wrap" TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/></StackPanel></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SuccessBrush}"/><Setter TargetName="border" Property="Background" Value="{StaticResource BgControlHoverBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Button.Template>
            </Button>

            <Button Command="{Binding AbrirReporteSaldosCommand}" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="10" Height="210" Cursor="Hand">
                <Button.Template><ControlTemplate TargetType="Button"><Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="20"><StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"><Border Background="#1AE74C3C" Width="58" Height="58" CornerRadius="29" Margin="0,0,0,14"><iconPacks:PackIconMaterial Kind="AccountCash" Width="29" Height="29" Foreground="#E74C3C" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="Saldos Pendientes" FontSize="18" FontWeight="Bold"/><TextBlock Text="Cargos, abonos y saldos reales por paciente, excluyendo cancelados." TextWrapping="Wrap" TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/></StackPanel></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="#E74C3C"/><Setter TargetName="border" Property="Background" Value="{StaticResource BgControlHoverBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Button.Template>
            </Button>
        </UniformGrid>
    </Grid>
</UserControl>
